## ADDED Requirements

### Requirement: Hover Highlighting State
The system SHALL visually distinguish the hovered node and its direct neighbors.

#### Scenario: Hovering over a node
- **WHEN** the user hovers over node A
- **THEN** node A's color changes to red.
- **AND** all nodes connected to node A by a link change their color to yellow.
